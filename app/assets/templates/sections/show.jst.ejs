<hr>
<div class="row section-show-title">
  <div class="col-md-12">
    <% if (section.has('title') && !section.get('hide_title')) { %>
      <h2><%= section.escape('title') %></h2>
    <% } %>
  </div>
</div>

<div class="rows-container">
</div>
